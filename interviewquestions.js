//difference b/w == and === operator?

var num1 = 5;
var num2 = '5';
console.log(typeof(num1));
console.log(typeof(num2));

console.log(num1 == num2); //true
console.log(num1 === num2); //false

//why fuctions?
//to reuse the code
//to avoid code duplication
//to make the code more readable
//define the code once and use it many times
//use the code many times with different arguments
//to produce diffrent results

//or

//a fuction is a group of reusable code which can be called anywhere in your program.
//it eliminates the need to write the same code again and again
